str_input = input("Enter string to find max character and min character: ")
print(f"Max character: {max(str_input)} value in ASCII TABLE: {ord(max(str_input))}")
print(f"Min character: {min(str_input)} value in ASCII TABLE: {ord(min(str_input))}")